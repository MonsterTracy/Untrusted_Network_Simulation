# Runtime configurations

Phase-1 Canonical Collection does not accept the superseded pilot or fixed-size
batch YAML profiles. A production run is bound to an immutable Collection Plan;
the formal command/config surface is introduced only with the planned CLI
commit.

`twd_tom_shadow_deepseek_v4_flash.yaml` remains only until the later planned
legacy-lineage deletion commit; it is not reachable from Canonical Collection.
